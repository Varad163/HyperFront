import React from 'react';

function Endpage() {
    return (
        <div>
            <video src={} autoPlay muted loop playsInline className="w-full h-auto" />
        </div>
    );
}

export default Endpage;
